{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "cd artifacts/capstone && BASE_PATH=/ PORT=5000 pnpm run build",
  "outputDirectory": "artifacts/capstone/dist/public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "${BACKEND_URL}/api/$1",
      "env": ["BACKEND_URL"]
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
